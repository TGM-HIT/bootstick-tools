# Always check in and check out the following files with LFs.
*.sh text eol=lf

# Always check in and check out the following files with CRLFs.
*.bat text eol=crlf
*.ps1 text eol=crlf
*.psd1 text eol=crlf
*.psm1 text eol=crlf
